# Kept for people who install with pip -r. The source of truth is pyproject.toml:
#   pip install -e ".[dev]"
# Run order: python -m churn.train, then python audit/fairness_audit.py.
# The notebooks are exploration and can be read in any order after that.
pandas>=2.2
numpy>=1.26
scikit-learn>=1.5
scipy>=1.13
matplotlib>=3.8
joblib>=1.4
openpyxl>=3.1        # pandas.read_excel needs this for BankChurners.xlsx
seaborn>=0.13        # EDA notebook only
jupyterlab>=4.0      # only needed to open the notebooks
pytest>=8.0
